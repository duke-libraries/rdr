<%# NOTE: Overrides existing Hyrax layout %>
<% content_for(:navbar) do %>
  <%# See Hyrax's native support for hyrax/homepage/marketing feature. %>
  <%# Revisit in future when we know requirements for the RDR homepage. %>
<% end %>

<% content_for(:precontainer_content) do %>
  <%= render 'hyrax/homepage/announcement' if controller_name == 'homepage' %>
<% end %>

<%= content_for(:page_title) do %>
  // Duke Digital Repository
<% end %>

<%= render template: 'layouts/hyrax' %>
